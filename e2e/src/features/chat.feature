# language: zh-CN
@cradle @P0 @CRADLE-CHAT-001
功能: 聊天功能
  作为用户，我希望能够发送消息给 Agent、处理中断与失败，并在刷新后继续看到真实状态

  背景:
    假如 应用已启动

  @smoke @CRADLE-CHAT-002
  场景: 新建聊天页面可见
    假如 我已配置 Mock LLM Provider
    当 我点击"新建聊天"导航项
    那么 我应该看到新建聊天页面
    而且 聊天输入框应可见

  @smoke @CRADLE-CHAT-003
  场景: 发送消息并接收已完成的 AI 回复
    假如 我已配置 Mock LLM Provider
    而且 我已添加了一个工作区
    而且 我已导航到新建聊天页面
    当 我在新建聊天输入框中输入"你好，请自我介绍"
    而且 我点击发送按钮
    那么 应该跳转到聊天视图
    而且 我应该看到用户消息"你好，请自我介绍"
    而且 聊天状态最终应为"idle"
    而且 最后一条 AI 消息应包含"Hello from mock LLM!"
    而且 最后一条 AI 消息持久化状态应为"complete"
    而且 聊天中不应出现错误提示

  @CRADLE-CHAT-004
  场景: 在聊天视图中继续发送消息
    假如 我已配置 Mock LLM Provider
    而且 我已添加了一个工作区
    而且 我已在新建聊天页面发送了初始消息
    当 我在聊天输入框中输入"继续说明"
    而且 我点击聊天发送按钮
    那么 我应该看到用户消息"继续说明"
    而且 聊天状态最终应为"idle"
    而且 最后一条 AI 消息应包含"Hello from mock LLM!"
    而且 最后一条 AI 消息持久化状态应为"complete"

  @CRADLE-CHAT-005
  场景: 发送消息后侧栏显示会话
    假如 我已配置 Mock LLM Provider
    而且 我已添加了一个工作区
    而且 我已在新建聊天页面发送了初始消息
    那么 聊天状态最终应为"idle"
    而且 侧栏应显示至少一个会话项

  @P1 @CRADLE-CHAT-006
  场景: 可以在流式回复进行中停止当前聊天
    假如 我已配置会慢速流式返回的 Mock LLM Provider
    而且 我已添加了一个工作区
    而且 我已导航到新建聊天页面
    当 我在新建聊天输入框中输入"请生成一段比较长的说明用于停止测试"
    而且 我点击发送按钮
    那么 应该跳转到聊天视图
    而且 聊天流应处于进行中
    当 我点击停止生成按钮
    那么 聊天状态最终应为"idle"
    而且 我应该看到至少一条 AI 消息
    而且 最后一条 AI 消息持久化状态应为"aborted"
    而且 聊天中不应出现错误提示

  @P1 @CRADLE-CHAT-007
  场景: Provider 失败时会显示错误状态
    假如 我已配置会失败的 Mock LLM Provider
    而且 我已添加了一个工作区
    而且 我已导航到新建聊天页面
    当 我在新建聊天输入框中输入"请触发一个 provider 错误"
    而且 我点击发送按钮
    那么 应该跳转到聊天视图
    而且 聊天状态最终应为"error"
    而且 聊天错误提示应显示"Mock LLM forced failure"
    而且 最后一条 AI 消息持久化状态应为"failed"

  @P1 @CRADLE-CHAT-008
  场景: 页面重载后可以恢复进行中的流式回复
    假如 我已配置会慢速流式返回的 Mock LLM Provider
    而且 我已添加了一个工作区
    而且 我已导航到新建聊天页面
    当 我在新建聊天输入框中输入"请输出一段足够长的说明，以便我在中途刷新页面"
    而且 我点击发送按钮
    那么 应该跳转到聊天视图
    而且 聊天流应处于进行中
    而且 我记录当前聊天会话标识
    当 我重新加载当前页面
    那么 当前聊天会话标识应保持不变
    而且 聊天状态最终应为"idle"
    而且 最后一条 AI 消息应包含"Hello from mock LLM!"
    而且 最后一条 AI 消息持久化状态应为"complete"
    而且 聊天中不应出现错误提示

  @P1 @CRADLE-CHAT-009
  场景: 首条消息会持久化 backend binding 与已完成 run
    假如 我已配置 Mock LLM Provider
    而且 我已添加了一个工作区
    而且 我已导航到新建聊天页面
    当 我在新建聊天输入框中输入"请建立一次 control plane 回归会话"
    而且 我点击发送按钮
    那么 应该跳转到聊天视图
    而且 聊天状态最终应为"idle"
    而且 当前聊天会话应持久化一条 backend binding
    而且 当前聊天会话应持久化一条状态为"complete"的 backend run

  @P1 @CRADLE-CHAT-010
  场景: 聊天回合会持久化 append-only backend timeline 事件
    假如 我已配置 Mock LLM Provider
    而且 我已添加了一个工作区
    而且 我已导航到新建聊天页面
    当 我在新建聊天输入框中输入"请建立一次 timeline 持久化回归会话"
    而且 我点击发送按钮
    那么 应该跳转到聊天视图
    而且 聊天状态最终应为"idle"
    而且 当前聊天会话应持久化 backend timeline 事件序列

  @P1 @CRADLE-CHAT-011
  场景: 多轮对话会把前序上下文发送给 Mock LLM
    假如 我已配置按轮次返回不同回复的 Mock LLM Provider
    而且 我已添加了一个工作区
    而且 我已导航到新建聊天页面
    当 我在新建聊天输入框中输入"第一轮问题：请记住苹果"
    而且 我点击发送按钮
    那么 应该跳转到聊天视图
    而且 聊天状态最终应为"idle"
    而且 最后一条 AI 消息应包含"第一轮助手回复：我记住了苹果。"
    当 我在聊天输入框中输入"第二轮问题：请记住香蕉"
    而且 我点击聊天发送按钮
    那么 聊天状态最终应为"idle"
    而且 最后一条 AI 消息应包含"第二轮助手回复：我记住了香蕉。"
    当 我在聊天输入框中输入"第三轮问题：请总结前两轮你记住了什么"
    而且 我点击聊天发送按钮
    那么 聊天状态最终应为"idle"
    而且 最后一条 AI 消息应包含"第三轮助手回复：你先让我记住苹果，又让我记住香蕉。"
    而且 第 3 次 Mock LLM 对话请求的 messages 应按顺序包含以下内容:
      | role      | content                                   |
      | user      | 第一轮问题：请记住苹果                   |
      | assistant | 第一轮助手回复：我记住了苹果。           |
      | user      | 第二轮问题：请记住香蕉                   |
      | assistant | 第二轮助手回复：我记住了香蕉。           |
      | user      | 第三轮问题：请总结前两轮你记住了什么     |

  @P1 @CRADLE-CHAT-012
  场景: 可以重命名已有会话
    假如 我已配置 Mock LLM Provider
    而且 我已添加了一个工作区
    当 我新建一个聊天会话并记住为"待重命名会话"，首条消息为"待重命名会话：请生成一条稍后会被改名的记录"
    而且 我打开会话"待重命名会话"的菜单
    而且 我点击会话"待重命名会话"的重命名菜单项
    而且 我将会话"待重命名会话"重命名为"重命名后的会话"
    那么 侧栏中的会话"待重命名会话"标题应为"重命名后的会话"
    而且 会话"待重命名会话"的数据库标题应为"重命名后的会话"

  @P1 @CRADLE-CHAT-013
  场景: 可以置顶并取消置顶会话
    假如 我已配置 Mock LLM Provider
    而且 我已添加了一个工作区
    当 我新建一个聊天会话并记住为"旧会话"，首条消息为"旧会话：稍后把我置顶"
    而且 我新建一个聊天会话并记住为"新会话"，首条消息为"新会话：保持默认顺序"
    那么 侧栏会话顺序应为"新会话"在"旧会话"之前
    当 我打开会话"旧会话"的菜单
    而且 我点击会话"旧会话"的置顶菜单项
    那么 会话"旧会话"应显示为已置顶
    而且 会话"旧会话"的数据库置顶状态应为"true"
    而且 侧栏会话顺序应为"旧会话"在"新会话"之前
    当 我打开会话"旧会话"的菜单
    而且 我点击会话"旧会话"的取消置顶菜单项
    那么 会话"旧会话"不应显示为已置顶
    而且 会话"旧会话"的数据库置顶状态应为"false"
    而且 侧栏会话顺序应为"新会话"在"旧会话"之前

  @P1 @CRADLE-CHAT-014
  场景: 可以删除会话并清理持久化数据
    假如 我已配置 Mock LLM Provider
    而且 我已添加了一个工作区
    当 我新建一个聊天会话并记住为"待删除会话"，首条消息为"待删除会话：请生成一条会被删除的记录"
    那么 侧栏应显示会话"待删除会话"
    当 我打开会话"待删除会话"的菜单
    而且 我点击会话"待删除会话"的删除菜单项
    那么 侧栏中不应显示会话"待删除会话"
    而且 会话"待删除会话"应已从数据库完全删除
    而且 被删除的会话"待删除会话"不应继续作为当前聊天视图显示

  @P1 @CRADLE-CHAT-015
  场景: 可以将会话复制为 Markdown 到剪贴板
    假如 我已配置用于 Markdown 导出的 Mock LLM Provider
    而且 我已添加了一个工作区
    当 我新建一个聊天会话并记住为"Markdown 会话"，首条消息为"导出 Markdown 测试标题"
    而且 我清空 Electron 剪贴板
    而且 我打开会话"Markdown 会话"的菜单
    而且 我点击会话"Markdown 会话"的复制 Markdown 菜单项
    那么 Electron 剪贴板中应包含以下 Markdown 片段:
      | # 导出 Markdown 测试标题         |
      | ## User                         |
      | 导出 Markdown 测试标题           |
      | ## Assistant                    |
      | Markdown 导出助手回复：请复制我。 |

  @P1 @CRADLE-CHAT-016
  场景: 可以展开查看最后一条 AI 消息的 Reasoning 内容
    假如 我已配置会返回 Reasoning 的 Mock LLM Provider
    而且 我已添加了一个工作区
    而且 我已导航到新建聊天页面
    当 我在新建聊天输入框中输入"请先思考再回答"
    而且 我点击发送按钮
    那么 应该跳转到聊天视图
    而且 聊天状态最终应为"idle"
    而且 最后一条 AI 消息应包含"Hello from mock LLM!"
    而且 最后一条 AI 消息应显示 Reasoning 入口
    当 我展开最后一条 AI 消息的 Reasoning
    那么 最后一条 AI 消息的 Reasoning 应包含"第一步分析问题"
    而且 当前聊天会话应持久化 reasoning 事件序列

  @P1 @CRADLE-CHAT-017
  场景: Tool Call 结果块会显示输入与输出占位
    假如 我已配置会返回 Tool Call 的 Mock LLM Provider
    而且 我已添加了一个工作区
    而且 我已导航到新建聊天页面
    当 我在新建聊天输入框中输入"请调用 read_file 工具"
    而且 我点击发送按钮
    那么 应该跳转到聊天视图
    而且 聊天状态最终应为"idle"
    而且 最后一条 AI 消息应显示名为"read_file"的 Tool Call
    当 我展开最后一条 AI 消息中名为"read_file"的 Tool Call
    那么 最后一条 AI 消息中名为"read_file"的 Tool Call 输入应包含"demo.txt"
    而且 最后一条 AI 消息中名为"read_file"的 Tool Call 输出应包含"Tool call emitted without runtime execution"
    而且 当前聊天会话应持久化 tool call 事件序列

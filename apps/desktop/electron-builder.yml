appId: com.cradle.app
productName: Cradle
directories:
  buildResources: ../../resources
  output: release
files:
  - dist/**/*
  - node_modules/velopack/**/*
  - node_modules/@neon-rs/load/**/*
extraResources:
  - from: ../server/dist/desktop-runtime
    to: server
    filter:
      - '**/*'
      - '!node_modules/**'
  # Keep pnpm's runtime node_modules layout intact; dependency selection is owned by @cradle/server.
  - from: ../server/dist/desktop-runtime/node_modules
    to: server/node_modules
    filter:
      - '**/*'
  - from: ../../packages/db/drizzle
    to: drizzle
    filter:
      - '**/*'
  - from: ../../plugins/browser-use
    to: plugins/browser-use
    filter:
      - package.json
      - dist/**/*
  - from: native/macos/mac-bridge/.build/cradle-dist
    to: mac-bridge
    filter:
      - '**/*'
asar: true
asarUnpack:
  - '**/*.node'
  - '**/*.wasm'
mac:
  category: public.app-category.developer-tools
  target:
    - dmg
    - zip
  entitlements: ../../build/entitlements.mac.plist
  entitlementsInherit: ../../build/entitlements.mac.plist
  hardenedRuntime: true
  gatekeeperAssess: false
win:
  target:
    - nsis
    - zip
  artifactName: ${productName}-${version}-${os}-${arch}.${ext}
linux:
  target:
    - AppImage
    - deb
  category: Development
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  artifactName: ${productName}-${version}-setup.${ext}
